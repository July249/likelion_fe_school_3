// 기본 틀
try {
  // code
} catch (errMsg) {
  // code
} finally {
  // code
}

// new Error(message);
// new SyntaxError(message);
// new ReferenceError(message);

try {
  let x = 10;
  let y = 20;
} catch (errMsg) {
} finally {
}
